{
  "name": "prospeccao-calendar-server",
  "version": "1.0.0",
  "description": "Servidor para integração Vapi + Google Calendar",
  "main": "server.js",
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider node server.js",
    "dev": "NODE_OPTIONS=--openssl-legacy-provider node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "googleapis": "^129.0.0",
    "node-cron": "^4.2.1"
  }
}
